#include "main.h"
/**
 *_strcmp - function that compares 2 strings
 *@s1: string 1
 *@s2: string 2
 *Return: 0
 */

int _strcmp(char *s1, char *s2)
{
int n = 0;
while (*(s1 + i) == *(s2 + i) && *(s1 + i))
i++;
if ((*s2 + i))
return (*(s1 + i) -*(s2 + i));
else
return (0);
}
